# The demo reads DEV_API_KEY from ../backend/.env or the container environment.
# No API key is needed in the frontend environment files.
# Backend address reachable from the browser.
VITE_API_BASE_URL=http://localhost:8080
# Optional server-side address for the same backend. Compose sets http://backend:8080.
# DISPO_BACKEND_URL=http://localhost:8080
